import { Person } from '../interfaces';

// add non configurable favoriteLanguage property, set to JavaScript
const exercise = (person: Person): void => {};

export default exercise;

// Oral Question
// What does configurable mean?// What does configurable mean?
