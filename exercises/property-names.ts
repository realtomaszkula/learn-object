import { Person } from '../interfaces';

// return enumerable property names
export const exercise1 = (person: Person): string[] => {
  return [];
};

// return enumerable and non-enumerable property names
export const exercise2 = (person: Person): string[] => {
  return [];
};
