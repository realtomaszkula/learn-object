import { Person } from '../interfaces';

// add non enumerable kink property, set to BDSM
const exercise = (person: Person): void => {};

export default exercise;

// Oral Question
// What does enumerable mean?
