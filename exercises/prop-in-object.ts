import { Person } from '../interfaces';

// check if firstName prop is in the person object (no inheritance)
export const exercise1 = (person: Person): boolean => {
  return null;
};

// check if firstName prop is in the specified person object or in its prototype chain (with inheritance)
export const exercise2 = (person: Person): boolean => {
  return null;
};
